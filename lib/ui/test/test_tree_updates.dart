import 'package:flutter/material.dart';
import 'package:youthetree/ui/organism/tree_updates.dart';

class TestTreeUpdates extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: TreeUpdates(),
    );
  }
}
